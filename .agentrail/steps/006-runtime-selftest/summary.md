Built a runnable self-test. selftest-scaffold.s provides 14 asm primitives + _start + test_thread that executes each of the 10 emitted fff_cfa_* colon defs with known inputs and EMITs recognizable byte sequences. test-kernel.sh concatenates scaffold + runtime-dict.s + dict_end and runs via cor24-run. Captured UART output: AA A ABA AB 1 7 @ R Z N DONE — all 10 defs compute correctly. tf24a_fff_selftest seeded and committed (reg-rs/*). All 67 tf24a tests pass.