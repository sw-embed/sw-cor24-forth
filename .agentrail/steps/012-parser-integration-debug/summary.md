Step 012 partial. Spent session reproducing the XC-COMPILE-TEST → XC-COMPILE-ENTRY integration failure from step 009. Confirmed bug is reproducible; confirmed all parser components work in isolation; confirmed 2-level WORD invocation alone doesn't trigger it; observed that direct invocation of XC-COMPILE-ENTRY (after manual XC-INIT-SYMBOLS) gets further than via XC-COMPILE-TEST. Did not land a fix. Findings + 5 hypotheses logged in forth-from-forth/docs/parser-debug-notes.md for the next session. 69 tests pass.