Step 009 partial. Dynamic-label infrastructure (NUM-C,, MAKE-CFA-LABEL, MAKE-ENTRY-LABEL, XC-COPY-COUNTED, XC-COMPILE-NAME-BYTES, XC-READ-TOKEN, XC-IS-SEMI?, XC-COMPILE-BODY, XC-COMPILE-ENTRY, XC-COMPILE-TEST) added to xcomp.fth (~140 lines). Each piece tested individually via phase-3 REPL and works. Integration via XC-COMPILE-TEST hits a phase-3 REPL corner case where IF/ELSE/THEN inside BEGIN/UNTIL returns 0 instead of the expected counted-string addr — context-dependent failure that doesn't reproduce in smaller test cases. All 67 tf24a tests still pass.