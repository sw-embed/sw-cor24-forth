Built the forth-from-forth cross-compiler plumbing: xcomp.fth defines C,, STR:, COUNT, TYPE, EMIT-COUNTED, NL + marker strings + a stub COMPILE-RUNTIME that emits '!!BEGIN-KERNEL!!\nhello from xcomp\n!!END-KERNEL!!' to UART. scripts/build-kernel.sh feeds the five core tiers + xcomp.fth + COMPILE-RUNTIME into cor24-run and captures the marker-delimited payload into forth-from-forth/compiler/out/runtime-dict.s. Confirmed end-to-end pipeline works. All 66 tf24a tests pass.