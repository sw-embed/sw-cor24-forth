Phase 1 bootstrap delivered: forth.s with entry point (r1=RSP at 0x0F0000, sp=DSP from hardware), inline test printing OK-newline-star-newline via data stack LIFO and return stack round-trip with TX busy-wait polling, plus EMIT and KEY primitives with NEXT tails ready for Phase 2 threading. Key discovery: fp register severely limited in ISA (no la/lcu/and), so primitives save IP on return stack and use r2 as temporary scratch for UART operations.