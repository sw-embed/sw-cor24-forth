Revised design after reversing Python choice. Cross-compiler is pure Forth (xcomp.fth) running on phase-3 REPL, emits COR24 assembler text via UART which shell redirection captures into forth-from-forth/kernel.s. No Python/C/Rust. Ship without hash table initially — Forth FIND linear walk is good enough for ~90 words; hash table in phase 3 served asm do_find which we're deleting. Design doc explains hashing mechanics (what/how/XMX/lookaside) and the escalation path if benchmarking later demands it: linear → first-char → 2-Round XMX.