Parser MVP: symbol-table + token dispatch. xcomp.fth grew ~130 lines: ?DUP helper, xc-symbols linked list, XC-REGISTER, XC-MATCH? (non-destructive name comparison), XC-FIND (walks list), XC-EMIT-TOKEN (3-way dispatch: label lookup → .word emission / number parse → LIT+n / else diagnostic). 5 primitive names registered. Verified interactively on phase-3 REPL: all lookups succeed, numbers parse correctly, unknowns emit as diagnostics. COMPILE-RUNTIME still uses hardcoded emitters; step 009 wires the parser in. All 67 tests pass.