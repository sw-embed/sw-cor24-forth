Step 005 — COMPILE-RUNTIME now emits real asm for runtime.fth's 10 colon defs (DUP DROP OVER SWAP R@ INVERT AND OR XOR NEGATE). 174 lines of asm output, assembles cleanly to 337 bytes via cor24-run --assemble, all labels resolve. xcomp.fth grew by ~400 lines: N., asm-emit helpers (EMIT-{BYTE,WORD}-LITERAL, EMIT-WORD-LABEL, EMIT-LABEL-DEF, EMIT-FAR-DOCOL), 23 counted-string label constants, 10 per-def emit helpers.