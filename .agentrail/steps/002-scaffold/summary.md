Scaffolded forth-from-forth/ as verbatim phase-3 copy: kernel.s byte-identical, five core/*.fth tiers byte-identical, four scripts/*.sh with retargeted header comments (demo.sh got -A 200 → -A 250 for fib capture). Added empty-shell compiler/xcomp.fth + core/prims.fth. Wrote phase-4 docs (prd, architecture, plan, status, kernel-sizes). Moved design.md from repo-root docs/ into forth-from-forth/docs/. Seeded tf24a_fff_fib reg-rs baseline with the full fib sequence in the captured window. All 66 tf24a tests pass (65 prior + new fff_fib).