Completed runtime.fth emission: all 13 defs now round-trip through the cross-compiler. Added EMIT-MINUS, EMIT-COLON, EMIT-SEMI. Added 8 new primitive-label constants (do_create, do_comma_docol, do_latest, do_rbrac, do_state, do_cfetch, do_cstore, do_comma). Added IMMEDIATE flag support (flags_len | 128). Expanded ['] into inline LIT+cfa emission. runtime-dict.s grows 174 → 240 lines. selftest-scaffold.s gets bra-self stubs for the new primitives so assembly resolves + a new MINUS test. Output: AA A ABA AB 1 7 @ R Z N A DONE. All 67 tf24a tests pass.