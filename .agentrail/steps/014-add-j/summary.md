Added J (outer-loop index in nested DO) as 1-line Forth def in all 3 layered kernels. Lifted RP@ as asm primitive into forth-in-forth (other 2 had it from subset 14A). Verified with NESTED 3 0 DO 2 0 DO I . J . LOOP LOOP test. New reg-rs test tf24a_fof_j. Also fixed parallel-mode flakiness in tf24a_fff_selftest by adding -t 60 to cor24-run invocations in build-kernel.sh and test-kernel.sh — default 10s wall-clock was getting hit under heavy parallel CPU contention. 71 tests pass.